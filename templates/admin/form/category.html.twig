{% extends 'base.html.twig' %}

{% block title %}Ajouter une categorie{% endblock %}

{% block body %}

{% if category is not defined %}
   <h1 class="text-center my-4">Ajouter une categorie</h1>
{% else %}
     <h1 class="text-center my-4">Modifier la categorie {{ category.name }}</h1>
{% endif %}     

<div class="row">
    <div class="col-4 mx-auto">
         {{ form(form) }}
    </div>
</div>

{% endblock %}